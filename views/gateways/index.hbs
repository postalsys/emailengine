{{#> ui/page-header title="Gateways" icon="icon-[tabler--send]" subtitle="Route outgoing email through your own SMTP servers or third-party providers." badgeLabel=total badgeVariant="neutral" class="mb-6"}}
{{> ui/btn href="/admin/gateways/new" label="Add Gateway" icon="icon-[tabler--plus]"}}
{{/ui/page-header}}

{{> ui/search-crumb parentHref="/admin/gateways" parentLabel="Gateways" query=query}}

{{#if gateways}}

<div class="card bg-base-100 shadow-md mb-6">

    <div class="overflow-x-auto">
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Emails Sent</th>
                    <th>Last Activity</th>
                    <th>Status</th>
                    <th class="w-12"><span class="sr-only">Actions</span></th>
                </tr>
            </thead>

            <tbody>
                {{#each gateways}}
                <tr class="hover:bg-base-200/60">
                    <td class="relative">
                        {{> ui/row-link href=(concat "/admin/gateways/gateway/" gateway) label="Open gateway"}}
                        <div><strong>{{name}}</strong></div>
                        {{> ui/entity-id id=gateway label="Gateway ID"}}
                    </td>
                    <td class="relative">
                        {{> ui/row-link href=(concat "/admin/gateways/gateway/" gateway)}}{{deliveries}}
                    </td>

                    <td class="relative">
                        {{> ui/row-link href=(concat "/admin/gateways/gateway/" gateway)}}
                        {{#if timeStr}}
                        <span class="relative-time" data-time="{{timeStr}}"></span>
                        {{else}}
                        <small><em>Never used</em></small>
                        {{/if}}
                    </td>

                    <td class="relative w-32">
                        {{> ui/row-link href=(concat "/admin/gateways/gateway/" gateway)}}
                        {{!-- live state badge with an error tooltip; wrapping link
                              lifts the badge above the row overlay --}}
                        <a href="/admin/gateways/gateway/{{gateway}}" class="relative z-[1] inline-flex">
                            {{> ui/state-badge label=label account=gateway pointer=true}}
                        </a>
                    </td>

                    <td class="relative text-end">
                        {{> ui/row-actions openHref=(concat "/admin/gateways/gateway/" gateway) label="Gateway actions"
                        deleteModal="#deleteGateway" deleteName=name deleteAction=(concat "/admin/gateways/delete/" gateway)}}
                    </td>

                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    {{> ui/pagination showPaging=showPaging prevPage=prevPage nextPage=nextPage pageLinks=pageLinks}}
</div>

{{else}}
{{> ui/list-empty noun="gateways" query=query firstPage=firstPage createHref="/admin/gateways/new" createLabel="Add your first gateway" clearHref="/admin/gateways"}}
{{/if}}

{{#> ui/details summary="How to use gateways" class="card bg-base-100 shadow-md mb-6"}}
<div>
    <p class="mb-4">A gateway is a shared SMTP account - your own mail server or a third-party service like SendGrid,
        Amazon SES, or Mailgun - that EmailEngine sends through on behalf of any account, instead of routing the
        message through that account's own email server.</p>
    <p class="mb-1"><strong>To route a message through a gateway:</strong></p>
    <ul class="list-disc ps-6 space-y-1 mb-4">
        <li>API: Add <code>gateway: "gateway-id"</code> to your <a
                href="https://learn.emailengine.app/docs/api/post-v-1-account-account-submit" target="_blank"
                rel="noopener noreferrer" referrerpolicy="no-referrer" class="link link-animated">submit request</a>
        </li>
        <li>SMTP: Include the header <code>X-EE-Gateway: gateway-id</code></li>
    </ul>
    <p class="mb-0"><a href="https://learn.emailengine.app/docs/sending/basic-sending#smtp-gateway" target="_blank"
            rel="noopener noreferrer" referrerpolicy="no-referrer" class="link link-animated">Learn more about
            gateways</a>.</p>
</div>
{{/ui/details}}

{{> ui/delete-modal id="deleteGateway" noun="gateway" action="/admin/gateways"}}
